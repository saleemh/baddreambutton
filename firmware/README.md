# Firmware

This folder is reserved for embedded software that runs on Bad Dream Button hardware.

Possible future contents:

- `button/`: firmware for the bedside button
- `gateway/`: firmware for an `ESP32`-class local receiver, if used
- hardware-specific build notes and flashing instructions
